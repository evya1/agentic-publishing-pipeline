# crews/

> **Status:** scaffold only — no Crew assembled yet.

Future home of CrewAI `Crew` assembly: wiring the agents from `../agents/`
to the tasks from `../tasks/` and selecting a `Process`.

The default planned `Process` is **sequential**. Any deviation must be
justified in `docs/PRD_crewai_pipeline.md`.
