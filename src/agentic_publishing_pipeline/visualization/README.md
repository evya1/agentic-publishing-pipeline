# visualization/

> **Status:** scaffold only — no graphs are generated yet.

Future home of Python code that produces at least one graph for inclusion in
the final PDF (satisfying the "at least one Python-generated graph" HW3
requirement).

Generated images will land under `../../../latex_project/figures/` (or
`../../../results/`, copied into `figures/` as needed). Inputs live under
`../../../assets/`. The plotting library and visual style will be chosen
when the first real graph is added.
