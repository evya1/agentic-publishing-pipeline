# results/

> **Status:** scaffold only — no generated artifacts exist yet.

This directory will hold artifacts produced by the pipeline:

- Generated graph images (from
  `../src/agentic_publishing_pipeline/visualization/`).
- Compiled article/book PDFs from `../latex_project/`.
- Any other build outputs that need to be inspected by humans before
  submission.

Raw, hand-curated inputs belong under [`../assets/`](../assets/), not here.

The final Moodle submission bundle lives under
[`../submission/`](../submission/).
