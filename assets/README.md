# assets/

> **Status:** scaffold only — no assets exist yet.

This directory will hold raw, static inputs used to build the article/book:

- Source images (before any LaTeX-specific resizing or conversion).
- Source data files for Python-generated graphs and tables.
- Any other static inputs that the pipeline consumes but does not generate.

Generated artifacts (compiled PDFs, exported graph images, etc.) belong under
[`../results/`](../results/), not here.

Do not commit fabricated or filler material.
