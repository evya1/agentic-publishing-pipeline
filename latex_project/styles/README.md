# latex_project/styles/

> **Status:** scaffold only — no custom style files exist yet.

This directory will hold custom `.sty` / `.cls` files or shared style
fragments (e.g., header/footer configuration, BiDi setup, theming) once the
document needs them.

Until then, document-wide packages are loaded directly from `../main.tex`.
