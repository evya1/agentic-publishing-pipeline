# latex_project/tables/

> **Status:** scaffold only — no tables exist yet.

This directory holds `.tex` fragments or data files for tables embedded into
the document. The final PDF must contain at least one real table.

Tables must be built from real data — fabricated rows / columns are
disallowed.
