% =============================================================================
% main.tex  --  PLACEHOLDER TEMPLATE ONLY.
%
% This file is intentionally minimal and contains NO real article content,
% NO citations, NO bibliography entries, NO included figures, NO tables, and NO
% formulas. Every required element is left as a TODO comment to be filled in
% later by the LaTeX Formatter agent (and reviewed by a human) once content
% has been approved in ../results/generated_markdown/.
%
% Compiler: prefer LuaLaTeX. XeLaTeX acceptable with documented justification.
% Bibliography: real .bib entries + biber/BibTeX. No fake citations.
% =============================================================================

\documentclass[11pt,a4paper]{article}

% --- Packages (to be expanded once the document gains real content) --------
% TODO: choose between babel + hebrew/english and polyglossia depending on
%       compiler choice, and load it here for BiDi support.
% TODO: load biblatex with backend=biber and a chosen style.
% TODO: load fancyhdr (or similar) for headers / footers.
% TODO: load graphicx for image inclusion.
% TODO: load amsmath / amssymb for mathematical formulas.
% TODO: load hyperref last for linked citations.

\begin{document}

% --- Cover sheet -----------------------------------------------------------
% TODO: build a real cover sheet listing:
%       - article/book topic (TBD)
%       - author/team members (TBD)
%       - course name and number
%       - submission date
%       Do NOT hardcode placeholder text into the final PDF.

% --- Table of contents -----------------------------------------------------
% TODO: \tableofcontents once real chapters exist.

% --- Headers and footers ---------------------------------------------------
% TODO: configure fancyhdr (or chosen alternative) so every body page has a
%       header and footer matching the document style.

% --- Chapters / sections ---------------------------------------------------
% TODO: \input{chapters/<chapter>.tex} for each approved chapter, after the
%       Markdown drafts in ../results/generated_markdown/ have been reviewed.

% --- At least one image ----------------------------------------------------
% TODO: \includegraphics from figures/ once a real image is added.

% --- At least one Python-generated graph -----------------------------------
% TODO: include the Phase 8 generated graph from figures/ once the Phase 9
%       planning chapter exists and owns the figure wrapper.

% --- At least one table ----------------------------------------------------
% TODO: real \begin{tabular}{...} ... \end{tabular} with real data.

% --- At least one mathematical formula -------------------------------------
% TODO: real \begin{equation} ... \end{equation} block.

% --- At least one Hebrew-English BiDi section -----------------------------
% TODO: demonstrate correct RTL (Hebrew) and LTR (English) rendering inside
%       the same section or chapter. Choice of mechanism depends on the
%       compiler (babel vs polyglossia).

% --- Bibliography ----------------------------------------------------------
% TODO: \printbibliography (biblatex) or \bibliography{references} (BibTeX)
%       once references.bib contains real entries.

% --- Linked citations ------------------------------------------------------
% TODO: ensure hyperref renders \cite{...} as clickable links to the
%       bibliography.

\end{document}
